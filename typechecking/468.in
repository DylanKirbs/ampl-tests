program InvalidFunctionReturnType:
    is_even(int x)-> bool array:
    int array arr;
    bool array arr2;
        if x * 2 = 0:
            return arr2
        else:
            return arr
            end
    main:
        bool b;
        let b = is_even(10)
