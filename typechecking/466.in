program IllegalArray:
    main:
    	int size;
    	int array arr;
    	let arr = array 4;
        let size = 98;
        let arr = array 4;
        let arr[0] = arr[0];
        output(arr) {illegal}
