program Chatbot:
    { fake program that doesn't actually work }
    greet:
        output("Hello! I am your friendly chatbot.")
        output("Ask me anything or type 'exit' to end the conversation.")

    main:
        greet()

        while true:
            let userMessage = input("You: ")
            
            if userMessage == "exit":
                output("Goodbye! Have a great day.")
                chillax
            else:
                output("Chatbot: I'm still learning, but I hear you. You said: '" .. userMessage .. "'")
        end

